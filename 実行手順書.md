# 実行手順書（Windows / Miniconda）

## 1. 初回準備

Anaconda PromptまたはPowerShellで次を実行する。

```powershell
C:\Users\banana\miniconda3\python.exe -m pip install pywin32
```

Excelファイルは処理前に閉じる。日本語・空白を含むパスは必ず引用符で囲む。

## 2. 自動テスト

```powershell
cd "C:\Dropbox\301_CodeX\業務管理について3"
C:\Users\banana\miniconda3\python.exe -m unittest discover -s tests -p "test_*.py" -v
```

## 3. JSON Export

```powershell
python -m task_management.cli export-json team "outputs\python_migration\latest\複数テーマ階層型タスク管理_Python連携版.xlsm" "exports\team.json"
python -m task_management.cli export-json personal "outputs\python_migration\latest\個人タスク管理_Python連携版.xlsm" "exports\personal.json"
```

## 4. Import Dry Run（必須）

```powershell
C:\Users\banana\miniconda3\python.exe -m task_management.cli import-json team "対象.xlsm" "exports\team.json" --dry-run
```

`missing_ids`、`missing_headers`、`rich_text_errors` がすべて空であることを確認する。

## 5. Import（Merge）

```powershell
python -m task_management.cli import-json team "対象.xlsm" "exports\team.json" --backup-dir "backup"
python -m task_management.cli import-json personal "outputs\python_migration\latest\個人タスク管理_Python連携版.xlsm" "exports\personal.json" --backup-dir "backup"
```

更新前にバックアップが作成される。JSONにない既存行は削除されない。

Import前検証でJSONにだけ存在する論理ID・通常列・週列を検出した場合、Dry Runでは追加予定件数として表示する。通常Importではバックアップ作成後、IDごとの新規行、同名の新規列、該当週の新規列を追加し、索引を再構築してから取り込む。類似ID・類似列への推測書込みは行わない。対象シート自体が存在しない場合、ID重複、Rich Text範囲矛盾の場合は更新せず中断する。

入力表を全消去してImportする場合も、新規行は3行目から最初の空行へ復元する。テンプレートの書式済み空行や見えない空文字を「最終データ行」として扱わない。UTC付きISO日時を含む旧JSONは、日本時間へ変換してExcel日付として復元する。

Excel上のボタンからImportする場合、ブックを開いたままでよい。バックアップはExcelの`SaveCopyAs`で作成するため、ExcelやDropboxがブックを開いていてもWindowsのファイルロックに依存しない。

ExcelボタンからのPython処理はバックグラウンド実行である。個人タスクを全消去した状態から11件を復元する実測では約30秒かかるため、開始メッセージ後すぐに結果を判断せず、`logs`にImport結果が出力され、`backup`にバックアップが作成されるまでブックを閉じない。Exportログしか存在しない場合はImport自体が起動していない。

## 6. v2→v3変換

```powershell
C:\Users\banana\miniconda3\python.exe -m task_management.cli migrate-v2 "旧v2.json" "新v3.json"
```

変換後はImport前に必ずDry Runする。旧v2には部分文字単位の書式がないため、その情報は新規に復元できない。

## 7. ID採番・整合性・進捗ログ

```powershell
C:\Users\banana\miniconda3\python.exe -m task_management.cli assign-ids team "対象.xlsm" --dry-run
C:\Users\banana\miniconda3\python.exe -m task_management.cli validate team "対象.xlsm"
C:\Users\banana\miniconda3\python.exe -m task_management.cli sync-progress team "対象.xlsm" --dry-run
```

`team`を`personal`へ変更すると個人版になる。実更新時は`--dry-run`を外し、必要に応じて`--backup-dir "backup"`を指定する。

## 8. Python連携版のボタン

`outputs/python_migration/latest`の連携版では、設定シートのJSON、バックアップ、ID採番、整合性確認、進捗ログ同期の対象ボタンがPythonへ切り替わっている。

ボタン処理はExcelがVBAマクロ実行中にCOMを占有しないよう、非同期で起動する。開始時に表示されるログファイルを確認する。処理中は同じブックを閉じたり、該当シートを編集したりしない。

連携版を移動する場合は、単独で移動せず、本プロジェクトの`task_management`フォルダから6階層以内へ置く。

## 9. 表示・ガント等のPythonコマンド

```powershell
C:\Users\banana\miniconda3\python.exe -m task_management.cli refresh-views personal "対象.xlsm" --dry-run
C:\Users\banana\miniconda3\python.exe -m task_management.cli refresh-gantt personal "対象.xlsm" --dry-run
C:\Users\banana\miniconda3\python.exe -m task_management.cli process-inbox "個人版.xlsm" --dry-run
C:\Users\banana\miniconda3\python.exe -m task_management.cli sync-completed "個人版.xlsm" --dry-run
C:\Users\banana\miniconda3\python.exe -m task_management.cli sync-gantt-dates team "チーム版.xlsm" --dry-run
```

`--dry-run`を外す前にExcelを閉じる。Excelボタンから起動する場合は、処理が非同期で動くため、ログ完了前にブックを閉じない。

## 10. 今回実施しない将来案

旧VBA削除とPythonのEXE化は実施しない。将来実施する場合も、Python版の実Excel回帰試験が完了した機能だけを削除対象とし、EXE化前にスクリプト版を正本として固定する。

## 11. JSON Import前の全データクリア（Python専用）

現在の運用データをすべて消してからJSONを取り込む場合にだけ使用する。Excel上のボタンやVBAからは呼び出せない。

最初に対象確認だけを行う場合：

```powershell
C:\Users\banana\miniconda3\python.exe -m task_management.cli clear-data personal "outputs\python_migration\latest\個人タスク管理_Python連携版.xlsm" --dry-run
```

実際に消去する場合：

```powershell
C:\Users\banana\miniconda3\python.exe -m task_management.cli clear-data personal "outputs\python_migration\latest\個人タスク管理_Python連携版.xlsm"
```

実行すると警告ポップアップが表示される。既定選択は「いいえ」であり、「はい」を明示的に選択した場合だけ処理する。消去前に設定シートの`エクスポートフォルダパス`へバックアップを作成する。

残るもの：シート、見出し、列構成、セル書式、設定シート、VBA、ボタン。消えるもの：タスク、テーマ、進捗、課題・リスク、意思決定、インボックス、完了ログ、派生一覧、ガントのタスク行と期間色。週・日などのガント見出しは残る。

チーム版では`personal`を`team`へ変更する。全データクリア後は、JSON Importを実行してから`refresh-all`を実行する。

## 12. 安全移行（全クリア＋Import＋検証＋失敗時復元）

通常は個別の全クリアとImportより、次の一括コマンドを優先する。対象Excelは必ず閉じてから実行する。

```powershell
C:\Users\banana\miniconda3\python.exe -m task_management.cli replace-import personal "outputs\python_migration\latest\個人タスク管理_Python連携版.xlsm" "取込対象.json" --dry-run
```

Dry Runが成功したら`--dry-run`を外す。警告ポップアップの既定選択は「いいえ」。

```powershell
C:\Users\banana\miniconda3\python.exe -m task_management.cli replace-import personal "outputs\python_migration\latest\個人タスク管理_Python連携版.xlsm" "取込対象.json"
```

このコマンドは、事前検証、バックアップ、全データクリア、JSON Import、セル値・週次部分書式の照合、全体更新、階層整合性検証を順番に実施する。途中で失敗した場合は元のバックアップを対象Excelへ戻す。結果レポートはバックアップ先へUTF-8のJSONで保存する。
# RefreshAll・自動保存（Python連携版）

## BATから実行する場合

Python連携版Excelと同じフォルダに、Miniconda Pythonを使用する次のBATがあります。

- `refresh_all.bat`
- `export_json.bat`
- `backup_workbook.bat`
- `import_json.bat`
- `validate_workbook.bat`
- `task_management_runner.bat`（共通ランナー）

`export_json.bat`、`backup_workbook.bat`、`refresh_all.bat`、`validate_workbook.bat`は、引数なしでダブルクリックすると同じフォルダの個人版・チーム版を両方処理します。

PowerShellまたはコマンドプロンプトで、ブック種別とブックパスを指定します。

```bat
refresh_all.bat personal "個人タスク管理_Python連携版.xlsm"
export_json.bat personal "個人タスク管理_Python連携版.xlsm"
backup_workbook.bat personal "個人タスク管理_Python連携版.xlsm"
validate_workbook.bat personal "個人タスク管理_Python連携版.xlsm"
import_json.bat personal "個人タスク管理_Python連携版.xlsm" "取込対象.json" --backup-dir "backup"
```

チーム版では `personal` を `team` に変更します。BATは `%USERPROFILE%\miniconda3\python.exe` を使用し、Pythonパッケージのあるプロジェクトルートへ自動移動します。Excel上のボタンも同じ `task_management_runner.bat` を使用します。そのため、Excelファイルと共通ランナーBATは同じフォルダに置いてください。

Excel上の「RefreshAll（全体更新）」ボタンはPython版を実行します。列の並び順、追加した独自列、列の非表示・グループ化状態は変更しません。派生シートの独自列の値は、論理ID（個人タスクID、タスクID、テーマID）で保持します。

設定シート（個人版は `07_設定`、チーム版は `09_設定`）の `エクスポートフォルダパス` に保存先を指定してください。絶対パスと、ブックの場所を基準にした相対パスを使用できます。空欄の場合はブックと同じ場所の `exports` フォルダを使用します。

コマンドラインで全体更新する場合：

```powershell
python -m task_management.cli refresh-all personal "対象ブック.xlsm"
python -m task_management.cli refresh-all team "対象ブック.xlsm"
```

出力先を指定せずにExport／Backupする場合：

```powershell
python -m task_management.cli export-auto personal "対象ブック.xlsm"
python -m task_management.cli backup-auto personal "対象ブック.xlsm"
```

Exportは `personal_YYYYMMDD_HHMMSS.json` または `team_YYYYMMDD_HHMMSS.json`、Backupは `元ブック名_backup_YYYYMMDD_HHMMSS_ffffff.xlsm` 形式で保存されます。
