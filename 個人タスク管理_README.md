# 個人タスク管理Excel

## 成果物

- `outputs/personal_task_manager/latest/個人タスク管理_最新版.xlsm`：実運用用ブック
- `personal_vba/`：VBAソース
- `個人タスク管理_テスト手順書.md`：会社PCでの受入・実行手順

## シート構成

| シート | 用途 |
|---|---|
| 01_個人タスク | 1行1アクションのマスター。IDは新規行だけ自動採番 |
| 02_今週 | 今週必須、通常予定、待ち、持ち越しを抽出 |
| 03_今日 | 今日・期限超過・優先度高・進行中を抽出し、見積時間を合計 |
| 04_予定 | 今週、来週、2週間後、3週間後、それ以降を一覧化 |
| 05_週次振り返り | 個人タスクを行、週を列方向へ配置。個人タスクID＋週で進捗文章を保持 |
| 06_完了ログ | 完了タスクをIDで重複防止して蓄積 |
| 07_設定 | 一覧、基準週、出力先、進捗開始列、バックアップ世代数 |
| 08_週ガント | 1列1週、列幅11で予定期間をセル塗りつぶし表示 |
| 09_日ガント | 年・月・日の3段見出し、1列1日、列幅4で表示 |
| 10_インボックス | 思いついた内容を素早く記録し、指定行を個人タスクへ変換 |
| 11_進捗ログ | 週次進捗を個人タスクID＋週で縦持ち保存 |
| 12_課題・リスク | タスクを妨げる課題・リスク、影響、対応方針を管理 |
| 13_意思決定 | 決定事項、理由、次アクション、見直し日を記録 |

## 初回セットアップ

1. `個人タスク管理.xlsm`を会社PCの書込み可能な場所へコピーします。
2. Excelで開き、「コンテンツの有効化」を押します。
3. `07_設定`の「エクスポートフォルダパス」を会社PC上の許可済みフォルダへ変更します。
4. 必要なら今週の開始日、標準1日作業時間、進捗開始列を変更します。
5. 「全体更新」ボタンを押します。全体更新前には自動バックアップされます。

## 日常運用

1. タスク発生時は`01_個人タスク`へタスク名と次アクションを入力します。
2. 毎週初めに予定開始日・期限・優先度を見直し、「全体更新」を実行します。
3. 毎朝`03_今日`を確認し、今日着手する内容は予定開始日を今日へ変更します。
4. 回答待ちは状態を「待ち」にし、待ち先・待ち期限を入力します。
5. 毎週末に「週次進捗更新」を実行して文章を追記し、「進捗ログ同期」を実行します。
6. Excel更新版へ移行するときは旧版でJSONエクスポート、新版でJSONインポートを実行します。JSON取込前には自動バックアップされます。

## マクロ概要

- `InitializeWorkbook`：書式・ボタンを再設定
- `AssignPersonalTaskIDs`：空欄IDだけを`PT_00001`形式で採番し、重複を検出
- `RefreshThisWeek` / `RefreshToday` / `RefreshSchedule`：マスターを壊さず表示シートを再生成
- `RefreshGantt`：週ガントと日ガントをセルの塗りつぶしで表示
- `GenerateWeeklyReview`：今週完了、持ち越し、期限超過、来週予定を抽出。既存文章は上書きしない
- `SyncCompletedTasks`：状態「完了」を完了ログへID単位で一度だけ追加
- `ProcessInbox`：処理状態が「タスク化」の行を個人タスクへ登録し、採番IDを戻す
- `SyncProgressLog`：週次進捗を個人タスクID＋週で重複なく縦持ち同期
- `AssignManagementIDs`：インボックス、課題・リスク、意思決定へIDを採番
- `ExportWorkbookJson` / `ImportWorkbookJson`：セル座標を使わず、行ID・列見出しで値と主要書式をUTF-8 JSONへ移行
- `BackupWorkbook`：時刻付きコピーを作成し、設定世代数を保持
- `RefreshAll`：バックアップ、採番、各表示更新、完了同期、書式更新を順に実行

## 非破壊・拡張設計

- 主要列は見出し名で検索します。途中へ列を追加しても見出し名を変更しなければ動作します。
- 既存ID、マスターのメモ、週次振り返り文章を更新処理で削除しません。
- JSONは個人タスクを「個人タスクID＋列見出し」、週次振り返りを「項目名＋週」、設定リストを「リスト見出し＋項目値」で照合します。行挿入や列移動後もセル位置に依存しません。
- 表示シートの非表示行は個人タスクID単位で退避・復元します。
- 緑・黄色による入力区分は2行目の見出しにだけ表示し、データセルは塗り分けません。
- 完了・中止行はグレー、期限超過と待ち期限超過は赤系で表示します。
- 「進捗開始列」は将来、週次振り返りへ補助列を増やす際の基準として設定に保持しています。

## 将来のチーム管理Excel連携案

`関連テーマID`と`関連チームタスクID`を外部キーとして使い、読取り専用でチーム側候補を取り込みます。個人タスクは複数件を同じチームタスクへ紐付け可能とし、個人側の完了をチーム側へ自動反映しません。将来追加する取込処理は、候補一覧の更新とID選択支援に限定するのが安全です。

## 個人タスクの親子階層

- `親タスクID`が空欄ならLevel 1（親タスクまたは単独タスク）、入力されていればLevel 2（子タスク）とPythonが自動判定します。
- ユーザーが入力するのは`親タスクID`だけです。`Level`列は確認用の自動項目なので入力しません。
- `02_今週`、`03_今日`、`04_予定`は、子を持つ親を除外し、子タスクと単独タスクを表示します。
- 週・日ガントは親子の両方を表示し、Level 1を濃い青、Level 2を青で区別します。期限超過・待ち・完了等の警告色を優先します。
- 週ガントの上部は年・月・週開始日の3段表示で、4行目には各週の月曜日の日だけを表示します。
- 親子関係は行番号ではなくIDで管理されるため、並べ替えや行挿入後も維持されます。
- JSONには`Level`と`親タスクID`も保存されます。旧JSONで両列がない場合はLevel 1として取り込みます。

## Python処理の完了通知

- ボタンを押した直後の開始ポップアップは表示しません。
- 処理中はExcelのステータスバーへ処理名を表示します。Excelは待機状態で固まりません。
- Pythonが正常終了すると「○○が完了しました」、異常終了するとログパス付きで「○○に失敗しました」と表示します。
- 処理実行中に別のPython処理を開始しようとした場合は、先の処理が終わるまで開始しません。
