# 受入テスト手順書

## 事前準備

1. `複数テーマ階層型タスク管理.xlsm`を任意の作業フォルダへコピーします。
2. Excelで開き、マクロを有効化します。
3. `09_設定`のエクスポートフォルダを、書込み可能なテスト用フォルダへ変更します。
4. 最初に「バックアップ」を実行します。

## 基本動作

1. 「階層チェック」を実行し、「問題はありません」と表示されることを確認します。
2. 「週ガント更新」「日ガント更新」を実行し、予定期間へ`■`が表示されることを確認します。
3. `05_週次進捗`のサンプル文章を確認後、「進捗ログ同期」を実行し、`06_進捗ログ`へ縦持ちで登録されることを確認します。
4. 各Python連携ボタンで開始ポップアップが出ず、処理中はステータスバー、終了時は完了ポップアップが表示されることを確認します。
5. 処理中に別のPython連携ボタンを押すと、同時実行せず警告されることを確認します。

## 指定8ケース

### ケース1・2：途中行追加と既存ID保持

1. `02_タスク`でL3_00002とL3_00003の間へ行を挿入します。
2. L3列だけに「追加確認用」を入力します。Level、親タスクID、テーマID、タスク名は空欄のままにします。
3. 「階層自動生成」を実行します。
4. 新規行だけに新しいL3 IDが付き、既存IDが変わらないことを確認します。

### ケース3：途中列追加

1. `02_タスク`の「担当」の前へ列を追加し、見出しを「確認用列」にします。
2. 「全体更新」を実行し、採番・ガント・進捗更新が完了することを確認します。

### ケース4・5：進捗文章の保持

1. `05_週次進捗`の過去週と現在週へ別々の文章を入力します。
2. `02_タスク`へL4タスクを追加し「週次進捗更新」を実行します。
3. 既存文章が両方残り、新規タスク行だけ増えることを確認します。
4. ガントを再生成しても進捗文章が変化しないことを確認します。

### ケース6：不正な親ID

1. テスト行の親タスクIDを`UNKNOWN_99999`へ変更します。
2. 「階層チェック」で行番号と「親タスクIDが存在しません」が表示されることを確認します。

### ケース7：ID重複

1. テスト行のタスクIDを既存IDと同じ値にします。
2. 「階層チェック」で重複が検出されることを確認します。

### ケース8：L5拡張

1. `02_タスク`でL4列の右へL5列を追加します。
2. `09_設定`の最大階層数を5へ変更します。
3. L5列だけに名称を設定した行を追加します。Level、L4親ID、テーマIDは空欄のままにします。
4. 「全体更新」でL5 IDと階層情報が自動生成され、日ガントと週次進捗にL5列が生成されることを確認します。

## JSON移行とバックアップ

1. 任意セルの文字色、背景色、太字、表示形式を変更します。
2. 「JSONエクスポート」を実行し、UTF-8の`.json`が出力されることを確認します。
3. 対象セルを変更後、「JSONインポート」を実行します。
4. 値と主要書式が復元され、取込直前の`.xlsm`バックアップも生成されることを確認します。
5. バックアップ数が「バックアップ世代数」を超えた場合、古いものから整理されることを確認します。
6. エクスポート後に対象行を挿入し、対象列の位置も移動してからインポートします。IDと列見出し名で値・書式が元の項目へ戻ることを確認します。

## 折り畳み・完了行

1. ガントまたは週次進捗で任意タスク行を非表示にします。
2. 対応する更新マクロを実行後も同じタスク行が非表示であることを確認します。
3. タスク状態を完了または中止に変更して「全体更新」を実行し、その行がグレーになることを確認します。
