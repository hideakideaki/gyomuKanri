# 個人タスク管理Excel 受入テスト・実行手順

## 事前準備

1. `個人タスク管理.xlsm`をテスト用フォルダへコピーします。
2. マクロを有効化します。
3. `07_設定`のエクスポートフォルダパスを、書込み可能なテスト用フォルダへ変更します。
4. 「バックアップ」を実行し、`.xlsm`コピーが作成されることを確認します。

## 基本確認

1. 「全体更新」を実行し、エラーなく完了することを確認します。
2. `02_今週`に分類付きのタスク、`03_今日`に今日の対象、`04_予定`に週区分が表示されることを確認します。
3. `01_個人タスク`で完了・中止行がグレー、期限超過・待ち期限超過セルが赤系になることを確認します。
4. 各Python連携ボタンで開始ポップアップが出ず、処理中はステータスバー、終了時は完了ポップアップが表示されることを確認します。
5. 処理中に別のPython連携ボタンを押すと、同時実行せず警告されることを確認します。

## 指定テストケース

1. 途中へ行を挿入し新規タスク名だけを入力後「タスクID採番」を実行します。既存IDが変わらず、新規行だけ採番されることを確認します。
2. 列を途中へ追加して見出し名を付け、「全体更新」が動くことを確認します。
3. 新規タスクを完了にし完了日を入力後、「完了ログ更新」を2回実行します。完了ログが1件だけ増えることを確認します。
4. 予定開始日を今週にして「今週更新」を実行し、表示されることを確認します。
5. 予定開始日を前週、状態を未完了にして、`02_今週`で「D. 持ち越し」になることを確認します。
6. 期限を昨日にして更新し、期限セルが赤くなることを確認します。
7. 状態を待ち、待ち期限を昨日にして更新し、待ち期限セルが赤くなることを確認します。
8. `05_週次振り返り`の文章欄へ任意文を入力後「週次振り返り生成」を再実行し、文章が消えないことを確認します。
9. `02_今週`等のタスク行を非表示にして更新し、同じ個人タスクIDの行が再び非表示になることを確認します。

## JSON移行確認

1. 任意セルの値、文字色、背景色、太字、表示形式を変更します。
2. 「JSONエクスポート」を実行し、UTF-8のJSONが作成されることを確認します。
3. `01_個人タスク`の途中へ行と列を挿入し、列見出しは維持します。
4. セル内容を変更後「JSONインポート」を実行し、行位置・列位置が変わっていても個人タスクIDと列見出しで値・主要書式が復元されることを確認します。
5. JSON本文にセル行番号・列番号をキーとする記録がないことを確認します。

## Minicondaについて

通常利用の主要ボタンはVBAブリッジ経由でMinicondaのPython処理を呼び出します。受入確認では同じ処理を下記コマンドから直接実行できます。

## 親子階層の確認

1. 親タスクIDを空欄にした親タスクを作成してIDを採番し、Levelが1になることを確認します。
2. 子タスクを作成して親タスクIDへ1のIDを入力し、全体更新後にLevelが2になることを確認します。
3. 親と子の予定開始日・期限を設定して全体更新します。
4. `02_今週`、`03_今日`、`04_予定`では子だけが表示され、子を持たないLevel 1は表示されることを確認します。
5. 週・日ガントでは親が濃い青、子が青で表示されることを確認します。
6. 存在しない親ID、Level 1への親ID設定、Level 2の親ID空欄について、検証コマンドがエラーを返すことを確認します。

Minicondaでの検証コマンド:

```bat
C:\Users\banana\miniconda3\python.exe -m task_management.cli validate personal "outputs\python_migration\latest\個人タスク管理_Python連携版.xlsm"
```
